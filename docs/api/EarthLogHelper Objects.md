## EarthLogHelper Objects

```python
class EarthLogHelper(BlueprintFunctionLibrary)
```

Earth Log Helper

**C++ Source:**

- **Plugin**: AesWorld
- **Module**: EarthLog
- **File**: EarthLogHelper.h

<a id="unreal.EarthLogHelper.warning_print_screen"></a>

#### warning\_print\_screen

```python
@classmethod
def warning_print_screen(cls,
                         key: int,
                         log_text: str,
                         prefix: str = "",
                         suffix: str = "") -> None
```

X.warning_print_screen(key, log_text, prefix="", suffix="") -> None
Warning Print Screen

Args:
    key (int32): 
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.warning_named_print_screen"></a>

#### warning\_named\_print\_screen

```python
@classmethod
def warning_named_print_screen(cls,
                               name: Name,
                               log_text: str,
                               prefix: str = "",
                               suffix: str = "") -> None
```

X.warning_named_print_screen(name, log_text, prefix="", suffix="") -> None
Warning Named Print Screen

Args:
    name (Name): 
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.warning"></a>

#### warning

```python
@classmethod
def warning(cls, log_text: str, prefix: str = "", suffix: str = "") -> None
```

X.warning(log_text, prefix="", suffix="") -> None
Warning

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.very_verbose"></a>

#### very\_verbose

```python
@classmethod
def very_verbose(cls,
                 log_text: str,
                 prefix: str = "",
                 suffix: str = "") -> None
```

X.very_verbose(log_text, prefix="", suffix="") -> None
Very Verbose

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.verbose"></a>

#### verbose

```python
@classmethod
def verbose(cls, log_text: str, prefix: str = "", suffix: str = "") -> None
```

X.verbose(log_text, prefix="", suffix="") -> None
Verbose

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.log"></a>

#### log

```python
@classmethod
def log(cls, log_text: str, prefix: str = "", suffix: str = "") -> None
```

X.log(log_text, prefix="", suffix="") -> None
Log

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.error_print_screen"></a>

#### error\_print\_screen

```python
@classmethod
def error_print_screen(cls,
                       key: int,
                       log_text: str,
                       prefix: str = "",
                       suffix: str = "") -> None
```

X.error_print_screen(key, log_text, prefix="", suffix="") -> None
Error Print Screen

Args:
    key (int32): 
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.error_named_print_screen"></a>

#### error\_named\_print\_screen

```python
@classmethod
def error_named_print_screen(cls,
                             name: Name,
                             log_text: str,
                             prefix: str = "",
                             suffix: str = "") -> None
```

X.error_named_print_screen(name, log_text, prefix="", suffix="") -> None
Error Named Print Screen

Args:
    name (Name): 
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.error"></a>

#### error

```python
@classmethod
def error(cls, log_text: str, prefix: str = "", suffix: str = "") -> None
```

X.error(log_text, prefix="", suffix="") -> None
Error

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.EarthLogHelper.display"></a>

#### display

```python
@classmethod
def display(cls, log_text: str, prefix: str = "", suffix: str = "") -> None
```

X.display(log_text, prefix="", suffix="") -> None
Display

Args:
    log_text (str): 
    prefix (str): 
    suffix (str):

<a id="unreal.AesGeoreference"></a>