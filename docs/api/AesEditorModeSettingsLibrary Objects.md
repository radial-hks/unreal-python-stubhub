## AesEditorModeSettingsLibrary Objects

```python
class AesEditorModeSettingsLibrary(BlueprintFunctionLibrary)
```

Aes Editor Mode Settings Library

**C++ Source:**

- **Plugin**: AesWorld
- **Module**: AesEditorMode
- **File**: AesEditorModeSettings.h

<a id="unreal.AesEditorModeSettingsLibrary.set_camera_speed_zoom"></a>

#### set\_camera\_speed\_zoom

```python
@classmethod
def set_camera_speed_zoom(cls, camera_speed_zoom: float) -> None
```

X.set_camera_speed_zoom(camera_speed_zoom) -> None
Set Camera Speed Zoom

Args:
    camera_speed_zoom (float):

<a id="unreal.AesEditorModeSettingsLibrary.set_camera_speed_scalar"></a>

#### set\_camera\_speed\_scalar

```python
@classmethod
def set_camera_speed_scalar(cls, camera_speed_scalar: float) -> None
```

X.set_camera_speed_scalar(camera_speed_scalar) -> None
Set Camera Speed Scalar

Args:
    camera_speed_scalar (float):

<a id="unreal.AesEditorModeSettingsLibrary.set_camera_speed"></a>

#### set\_camera\_speed

```python
@classmethod
def set_camera_speed(cls, camera_speed: int) -> None
```

X.set_camera_speed(camera_speed) -> None
Set Camera Speed

Args:
    camera_speed (int32):

<a id="unreal.AesEditorModeSettingsLibrary.get_camera_speed_zoom"></a>

#### get\_camera\_speed\_zoom

```python
@classmethod
def get_camera_speed_zoom(cls) -> float
```

X.get_camera_speed_zoom() -> float
Get Camera Speed Zoom

Returns:
    float:

<a id="unreal.AesEditorModeSettingsLibrary.get_camera_speed_scalar"></a>

#### get\_camera\_speed\_scalar

```python
@classmethod
def get_camera_speed_scalar(cls) -> float
```

X.get_camera_speed_scalar() -> float
Get Camera Speed Scalar

Returns:
    float:

<a id="unreal.AesEditorModeSettingsLibrary.get_camera_speed"></a>

#### get\_camera\_speed

```python
@classmethod
def get_camera_speed(cls) -> int
```

X.get_camera_speed() -> int32
Get Camera Speed

Returns:
    int32:

<a id="unreal.AesEditorPawnInterface"></a>