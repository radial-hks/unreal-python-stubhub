## EffectsAtom Objects

```python
class EffectsAtom(EntityAtomBase)
```

Effects Atom

**C++ Source:**

- **Plugin**: WdpAssetLoader
- **Module**: EffectsAssetLoader
- **File**: EffectsAtom.h

**Editor Properties:** (see get_editor_property/set_editor_property)

- ``active`` (bool):  [Read-Only]
- ``bool_parameters`` (Map[Name, bool]):  [Read-Only]
- ``color_parameters`` (Map[Name, LinearColor]):  [Read-Only]
- ``float_parameters`` (Map[Name, float]):  [Read-Only]
- ``int_parameters`` (Map[Name, int32]):  [Read-Only]
- ``speed`` (float):  [Read-Only]
- ``vector_parameters`` (Map[Name, Vector]):  [Read-Only]

<a id="unreal.SkeletalMeshAtom"></a>