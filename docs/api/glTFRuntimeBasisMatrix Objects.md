## glTFRuntimeBasisMatrix Objects

```python
class glTFRuntimeBasisMatrix(StructBase)
```

Gl TFRuntime Basis Matrix

**C++ Source:**

- **Plugin**: AesWorld
- **Module**: glTFRuntime
- **File**: glTFRuntimeParser.h

**Editor Properties:** (see get_editor_property/set_editor_property)

- ``origin`` (Vector):  [Read-Write]
- ``x_axis`` (Vector):  [Read-Write]
- ``y_axis`` (Vector):  [Read-Write]
- ``z_axis`` (Vector):  [Read-Write]

<a id="unreal.glTFRuntimeBasisMatrix.__init__"></a>

#### \_\_init\_\_

```python
def __init__(x_axis: Vector = [0.000000, 0.000000, 0.000000],
             y_axis: Vector = [0.000000, 0.000000, 0.000000],
             z_axis: Vector = [0.000000, 0.000000, 0.000000],
             origin: Vector = [0.000000, 0.000000, 0.000000]) -> None
```

<a id="unreal.glTFRuntimeBasisMatrix.x_axis"></a>

#### x\_axis

```python
@property
def x_axis() -> Vector
```

(Vector):  [Read-Write]

<a id="unreal.glTFRuntimeBasisMatrix.x_axis"></a>

#### x\_axis

```python
@x_axis.setter
def x_axis(value: Vector) -> None
```

<a id="unreal.glTFRuntimeBasisMatrix.y_axis"></a>

#### y\_axis

```python
@property
def y_axis() -> Vector
```

(Vector):  [Read-Write]

<a id="unreal.glTFRuntimeBasisMatrix.y_axis"></a>

#### y\_axis

```python
@y_axis.setter
def y_axis(value: Vector) -> None
```

<a id="unreal.glTFRuntimeBasisMatrix.z_axis"></a>

#### z\_axis

```python
@property
def z_axis() -> Vector
```

(Vector):  [Read-Write]

<a id="unreal.glTFRuntimeBasisMatrix.z_axis"></a>

#### z\_axis

```python
@z_axis.setter
def z_axis(value: Vector) -> None
```

<a id="unreal.glTFRuntimeBasisMatrix.origin"></a>

#### origin

```python
@property
def origin() -> Vector
```

(Vector):  [Read-Write]

<a id="unreal.glTFRuntimeBasisMatrix.origin"></a>

#### origin

```python
@origin.setter
def origin(value: Vector) -> None
```

<a id="unreal.glTFRuntimeMeshLOD"></a>