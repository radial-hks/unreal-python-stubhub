## EnvironmentUtilManager Objects

```python
class EnvironmentUtilManager(Object)
```

用于添加时间同步等相关的接口函数和变量

**C++ Source:**

- **Plugin**: WdpEnvironment
- **Module**: WdpEnvironment
- **File**: EnvironmentUtilManager.h

<a id="unreal.EnvironmentUtilManager.set_device_time_sync_is_allowed"></a>

#### set\_device\_time\_sync\_is\_allowed

```python
def set_device_time_sync_is_allowed(is_allowed: bool) -> None
```

x.set_device_time_sync_is_allowed(is_allowed) -> None
设置是否开启设备时间同步

Args:
    is_allowed (bool):

<a id="unreal.EnvironmentUtilManager.get_device_time_sync_is_allowed"></a>

#### get\_device\_time\_sync\_is\_allowed

```python
def get_device_time_sync_is_allowed() -> bool
```

x.get_device_time_sync_is_allowed() -> bool
获取是否开启设备时间同步

Returns:
    bool:

<a id="unreal.RealtimeWeatherManager"></a>