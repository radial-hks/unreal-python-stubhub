## TransformAtomBase Objects

```python
class TransformAtomBase(EntityAtomBase)
```

Transform Atom Base

**C++ Source:**

- **Plugin**: WdpDataModel
- **Module**: WdpBasicAtoms
- **File**: TransformAtomBase.h

<a id="unreal.TransformAtomBase.set_view_transform"></a>

#### set\_view\_transform

```python
def set_view_transform(new_transform: Transform) -> bool
```

x.set_view_transform(new_transform) -> bool
Set View Transform

Args:
    new_transform (Transform): 

Returns:
    bool:

<a id="unreal.TransformAtomBase.set_scale3d"></a>

#### set\_scale3d

```python
def set_scale3d(new_scale: Vector) -> bool
```

x.set_scale3d(new_scale) -> bool
Set Scale 3d

Args:
    new_scale (Vector): 

Returns:
    bool:

<a id="unreal.TransformAtomBase.set_rotator"></a>

#### set\_rotator

```python
def set_rotator(new_rotator: Rotator) -> bool
```

x.set_rotator(new_rotator) -> bool
Set Rotator

Args:
    new_rotator (Rotator): 

Returns:
    bool:

<a id="unreal.TransformAtomBase.set_pivot_offset"></a>

#### set\_pivot\_offset

```python
def set_pivot_offset(offset: Vector) -> bool
```

x.set_pivot_offset(offset) -> bool
Set Pivot Offset

Args:
    offset (Vector): 

Returns:
    bool:

<a id="unreal.TransformAtomBase.set_location"></a>

#### set\_location

```python
def set_location(new_location: Vector) -> bool
```

x.set_location(new_location) -> bool
Set Location

Args:
    new_location (Vector): 

Returns:
    bool:

<a id="unreal.TransformAtomBase.get_view_transform"></a>

#### get\_view\_transform

```python
def get_view_transform() -> Transform
```

x.get_view_transform() -> Transform
Get View Transform

Returns:
    Transform:

<a id="unreal.TransformAtomBase.get_scale3d"></a>

#### get\_scale3d

```python
def get_scale3d() -> Vector
```

x.get_scale3d() -> Vector
Get Scale 3d

Returns:
    Vector:

<a id="unreal.TransformAtomBase.get_rotator_data"></a>

#### get\_rotator\_data

```python
def get_rotator_data() -> Rotator
```

x.get_rotator_data() -> Rotator
Get Rotator Data

Returns:
    Rotator:

<a id="unreal.TransformAtomBase.get_pivot_transform"></a>

#### get\_pivot\_transform

```python
def get_pivot_transform() -> Transform
```

x.get_pivot_transform() -> Transform
Get Pivot Transform

Returns:
    Transform:

<a id="unreal.TransformAtomBase.get_pivot_offset_data"></a>

#### get\_pivot\_offset\_data

```python
def get_pivot_offset_data() -> Vector
```

x.get_pivot_offset_data() -> Vector
Get Pivot Offset Data

Returns:
    Vector:

<a id="unreal.TransformAtomBase.get_pivot"></a>

#### get\_pivot

```python
def get_pivot() -> Vector
```

x.get_pivot() -> Vector
Get Pivot

Returns:
    Vector:

<a id="unreal.TransformAtomBase.get_location_data"></a>

#### get\_location\_data

```python
def get_location_data() -> Vector
```

x.get_location_data() -> Vector
Get Location Data

Returns:
    Vector:

<a id="unreal.TransformAtomBase.get_data_transform"></a>

#### get\_data\_transform

```python
def get_data_transform() -> Transform
```

x.get_data_transform() -> Transform
Get Data Transform

Returns:
    Transform:

<a id="unreal.TransformAtomBase.can_scale"></a>

#### can\_scale

```python
def can_scale() -> bool
```

x.can_scale() -> bool
Can Scale

Returns:
    bool:

<a id="unreal.TransformAtomBase.can_rotate"></a>

#### can\_rotate

```python
def can_rotate() -> bool
```

x.can_rotate() -> bool
Can Rotate

Returns:
    bool:

<a id="unreal.TransformAtomBase.can_move"></a>

#### can\_move

```python
def can_move() -> bool
```

x.can_move() -> bool
Can Move

Returns:
    bool:

<a id="unreal.TransformAtom"></a>