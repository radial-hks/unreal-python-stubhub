## UrbanWorldBuildManager Objects

```python
class UrbanWorldBuildManager(Object)
```

Urban World Build Manager

**C++ Source:**

- **Plugin**: AesBuilderCommon
- **Module**: UrbanWorldBuilder
- **File**: UrbanWorldBuilderManager.h

<a id="unreal.UrbanWorldBuildManager.get_build_system"></a>

#### get\_build\_system

```python
def get_build_system(target_level: int, layer_name: str) -> UrbanBuildSystem
```

x.get_build_system(target_level, layer_name) -> UrbanBuildSystem
Get Build System

Args:
    target_level (int32): 
    layer_name (str): 

Returns:
    UrbanBuildSystem:

<a id="unreal.UrbanWorldPreviewComponent"></a>