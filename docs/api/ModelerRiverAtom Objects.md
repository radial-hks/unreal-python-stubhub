## ModelerRiverAtom Objects

```python
class ModelerRiverAtom(EntityAtomBase)
```

Modeler River Atom

**C++ Source:**

- **Plugin**: AesRuntimeModeler
- **Module**: WdpModelerEntity
- **File**: ModelerRiverAtom.h

**Editor Properties:** (see get_editor_property/set_editor_property)

- ``corner_radius`` (float):  [Read-Only]
- ``corner_split_num`` (int32):  [Read-Only]
- ``inverse_normal`` (bool):  [Read-Only]
- ``merge_length`` (float):  [Read-Only]
- ``ripple_scale`` (float):  [Read-Only]
- ``river_height`` (float):  [Read-Only]
- ``river_width`` (float):  [Read-Only]
- ``use_vertex_corner`` (bool):  [Read-Only]
- ``use_vertex_width`` (bool):  [Read-Only]
- ``water_color`` (str):  [Read-Only]
- ``water_transparency`` (float):  [Read-Only]
- ``water_transparent_depth`` (float):  [Read-Only]
- ``water_type`` (WdpModelerRiverType):  [Read-Only]
- ``wave_height`` (float):  [Read-Only]
- ``wave_rotate`` (float):  [Read-Only]
- ``wave_speed`` (float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.merge_length"></a>

#### merge\_length

```python
@property
def merge_length() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.river_width"></a>

#### river\_width

```python
@property
def river_width() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.river_height"></a>

#### river\_height

```python
@property
def river_height() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.inverse_normal"></a>

#### inverse\_normal

```python
@property
def inverse_normal() -> bool
```

(bool):  [Read-Only]

<a id="unreal.ModelerRiverAtom.corner_radius"></a>

#### corner\_radius

```python
@property
def corner_radius() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.corner_split_num"></a>

#### corner\_split\_num

```python
@property
def corner_split_num() -> int
```

(int32):  [Read-Only]

<a id="unreal.ModelerRiverAtom.use_vertex_corner"></a>

#### use\_vertex\_corner

```python
@property
def use_vertex_corner() -> bool
```

(bool):  [Read-Only]

<a id="unreal.ModelerRiverAtom.use_vertex_width"></a>

#### use\_vertex\_width

```python
@property
def use_vertex_width() -> bool
```

(bool):  [Read-Only]

<a id="unreal.ModelerRiverAtom.water_type"></a>

#### water\_type

```python
@property
def water_type() -> WdpModelerRiverType
```

(WdpModelerRiverType):  [Read-Only]

<a id="unreal.ModelerRiverAtom.water_color"></a>

#### water\_color

```python
@property
def water_color() -> str
```

(str):  [Read-Only]

<a id="unreal.ModelerRiverAtom.wave_height"></a>

#### wave\_height

```python
@property
def wave_height() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.water_transparency"></a>

#### water\_transparency

```python
@property
def water_transparency() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.water_transparent_depth"></a>

#### water\_transparent\_depth

```python
@property
def water_transparent_depth() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.ripple_scale"></a>

#### ripple\_scale

```python
@property
def ripple_scale() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.wave_rotate"></a>

#### wave\_rotate

```python
@property
def wave_rotate() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerRiverAtom.wave_speed"></a>

#### wave\_speed

```python
@property
def wave_speed() -> float
```

(float):  [Read-Only]

<a id="unreal.ModelerWaterAtom"></a>